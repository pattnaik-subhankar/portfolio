# Flow Diagrams

End-to-end architecture flows for the systems that actually ran (generated 2026-08-07).

- `01-agent-ecosystem` — 150 agents, 4 maturity layers, shared llm_provider
- `02-agent-lifecycle` — the one shared agent pattern (input → logic → llm_provider → output → push)
- `03-product-pipeline` — Mental Performance OS: research → PDFs → landing pages → GitHub Pages → subhankarcloud.in
- `04-maturity-dashboard` — weekly cron → scoring → dashboard + report → git push → Telegram
- `05-email-funnel` — capture page → FormSubmit → inbox → 5-email drip → checkout
- `06-geospatial` — PROJECT GEO-SPATIAL: Notion → research → plan → financials → playbook → decisions
- `07-function-compute-deployment` — managed website delivery: start → build → projects.json → preview → publish

Each flow exists as `.svg` (scalable) and `.png` (2x render).
